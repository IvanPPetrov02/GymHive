---
# Membership Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: membership-service
  namespace: gymhive
  labels:
    app: membership-service
spec:
  replicas: 2  # Start with 2 replicas for HA
  selector:
    matchLabels:
      app: membership-service
  template:
    metadata:
      labels:
        app: membership-service
    spec:
      containers:
      - name: membership-service
        image: ivanppetrov/gymhive-membership-service:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:8080"
        - name: ConnectionStrings__DefaultConnection
          value: "Server=membership-db;Port=3306;Database=GymHiveGyms;User=gymhive_user;Password=GymHive123!;"
        - name: AppSettings__Token
          value: "YourSuperSecretKeyThatIsAtLeast32CharactersLong!"
        - name: GymService__BaseUrl
          value: "http://gym-service:8080"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
# Membership Service
apiVersion: v1
kind: Service
metadata:
  name: membership-service
  namespace: gymhive
spec:
  selector:
    app: membership-service
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
---
# Horizontal Pod Autoscaler for Membership Service
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: membership-service-hpa
  namespace: gymhive
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: membership-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
