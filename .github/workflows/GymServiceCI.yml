name: Gym Service CI

on:
  push:
    branches: [ main, dev ]
    paths:
      - 'GymHiveBackend/GymService/**'
      - '.github/workflows/GymServiceCI.yml'
  pull_request:
    branches: [ main, dev ]
    paths:
      - 'GymHiveBackend/GymService/**'
      - '.github/workflows/GymServiceCI.yml'
  workflow_dispatch:

concurrency:
  group: gym-service-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test Gym Service
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: GymHiveBackend/GymService
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build Solution
        run: dotnet build --configuration Release --no-restore

      - name: Run Tests
        run: dotnet test --configuration Release --no-build --verbosity normal --collect:"XPlat Code Coverage"
        continue-on-error: true

      - name: Publish Application
        run: dotnet publish --configuration Release --no-build --output ./publish

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: gym-service-build
          path: GymHiveBackend/GymService/publish/
          retention-days: 7

  docker-build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push'
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./GymHiveBackend
          file: ./GymHiveBackend/GymService/Dockerfile
          push: false
          tags: gymhive-gym-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: GymHiveBackend/GymService
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore Dependencies
        run: dotnet restore

      - name: Run Code Analysis
        run: dotnet build --configuration Release --no-restore /p:TreatWarningsAsErrors=false
