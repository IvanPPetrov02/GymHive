# ci: trivial pipeline bump comment
name: Gym Service CI

on:
  push:
    branches: [ main, dev ]
    paths:
      - 'GymHiveBackend/GymService/**'
      - '.github/workflows/GymServiceCI.yml'
  pull_request:
    branches: [ main, dev ]
    paths:
      - 'GymHiveBackend/GymService/**'
      - '.github/workflows/GymServiceCI.yml'
  workflow_dispatch:

concurrency:
  group: gym-service-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test Gym Service
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: GymHiveBackend/GymService
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build Solution
        run: dotnet build --configuration Release --no-restore

      - name: Run Tests
        run: dotnet test --configuration Release --no-build --verbosity normal --collect:"XPlat Code Coverage"
        continue-on-error: true

      - name: Publish Application
        run: dotnet publish --configuration Release --no-build --output ./publish

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: gym-service-build
          path: GymHiveBackend/GymService/publish/
          retention-days: 7

  docker-build-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push'
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/gymhive-gym-service
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev' }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./GymHiveBackend
          file: ./GymHiveBackend/GymService/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }},${{ secrets.DOCKER_USERNAME }}/gymhive-gym-service:${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      # IMAGE is not set here (push-only workflows mirror AuthenticationService CI)

